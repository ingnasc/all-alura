const alunos = ['João', 'Juliana', 'Caio', 'Ana'];
const medias = [10, 8, 7.5, 9];

const lista = [alunos, medias];

console.log(lista);

console.log(
    `A aluna na posição 1 da lista é: ${lista[0][1]},
    e sua nota foi ${lista[1][1]}`
);
